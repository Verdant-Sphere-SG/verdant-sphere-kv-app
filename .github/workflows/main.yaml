name: CI Workflow
run-name: ${{github.actor}} pushed changes.

# trigger
on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yaml

  test:
    name: Test
    uses: ./.github/workflows/test.yaml


   